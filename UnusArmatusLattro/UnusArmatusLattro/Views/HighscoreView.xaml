<UserControl x:Class="UnusArmatusLattro.Views.HighscoreView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UnusArmatusLattro.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="Transparent">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <TextBlock Text="{Binding Name, RelativeSource={RelativeSource AncestorType=UserControl}}" Grid.Column="0" FontSize="20" Margin="20,0,0,0"></TextBlock>
        <TextBlock Text="{Binding Score, RelativeSource={RelativeSource AncestorType=UserControl}}" Grid.Column="1" FontSize="20" FontWeight="Bold"></TextBlock>
    </Grid>
</UserControl>
